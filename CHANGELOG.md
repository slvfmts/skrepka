# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — по
[SemVer](https://semver.org/lang/ru/). До 1.0 публичный контракт может меняться.

## [Unreleased]

Предрелиз 0.9. Первый публичный срез. Ещё не опубликован в PyPI.

### Основное

- **Комментарии из CLI** — `comments` (чтение с пагинацией), `reply`, `comment`,
  `suggestions`. Резолв (`resolve` / `reply --resolve`) требует подтверждения человека;
  агент не резолвит сам.
- **Якорно-безопасные правки текста** — `patch` через `replaceAllText` с картой якорей
  из DOCX-экспорта: правки, полностью накрывающие якорь живого комментария, отклоняются
  (иначе тред стал бы «призраком») с подсказкой, как переписать; частичное перекрытие
  разрешено.
- **Загрузка и выгрузка** — `upload` / `update` (markdown → документ) и `download`
  (документ → markdown). Деструктивное полное обновление документа с комментариями
  блокируется, пока не подтверждено явно (с автоматическим бэкапом).
- **Мастер настройки** — `init` (пошаговая авторизация в Google, свой OAuth-клиент,
  smoke-тест) и `doctor` (диагностика кредов, токена, scope, включённости API).
- **Управление локальными данными** — `logout` (убрать токен), `revoke` (отозвать на
  стороне Google, server-first, с честным «результат неясен» при неоднозначном ответе),
  `forget` (best-effort удаление артефактов).
- **`sync`** (markdown → документ, three-way merge) — присутствует как
  **экспериментальный**, не входит в поддержанный сценарий 0.9.

### Безопасность

- Запрашивается **единственный** OAuth-scope `.../auth/drive` (избыточный `documents`
  убран).
- Все артефакты пишутся через защищённый I/O (`O_NOFOLLOW` + атомарный `rename`):
  подменённый по предсказуемому пути симлинк/хардлинк не приводит к записи сквозь него.
- Токен — `0600` в конфиг-директории `0700`; рефреш с урезанным scope не сохраняется.
- Резолв комментария и деструктивное полное обновление документа требуют подтверждения
  в TTY (неинтерактивный обход — `SKREPKA_ASSUME_HUMAN=1`). Команды управления данными
  `revoke`/`forget` имеют собственный `--yes`. Это кооперативные границы для агента
  (см. [PRIVACY.md](PRIVACY.md)/[SECURITY.md](SECURITY.md)), а не криптографические
  гарантии.
- Нет сервера, нет телеметрии. См. [PRIVACY.md](PRIVACY.md) и [SECURITY.md](SECURITY.md).

### Известно

- Движок прошёл многораундовое кросс-модельное ревью кода и живую характеризацию на
  настоящем Google Docs API (см. [docs/FINDINGS.md](docs/FINDINGS.md)).
- Ограничения 0.9 (single-tab, таблицы/сноски только в UI и др.) — в
  [docs/LIMITATIONS.md](docs/LIMITATIONS.md).
- Платформа — Unix; Windows в 0.9 не поддержан.
